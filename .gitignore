# macOS
.DS_Store

# Local config — never committed (CONFIG-3). Production config.js is
# generated at deploy time by .github/workflows/deploy.yml from repo
# secrets, then served from the GitHub Pages artifact only.
config.js

# Personal handoff notes
HANDOFF.md

# Editor / OS noise
*.swp
*.swo
*~
.idea/
.vscode/

# Node (in case anyone runs tooling locally)
node_modules/
npm-debug.log*

# Environment files
.env
.env.local
.env.*.local

# Logs
*.log
.claude/
